: main
    { 6 3 5 9 3 }list
    0 array_sort
    array_vals
    5 =
    swap 9 = and
    swap 6 = and
    swap 5 = and
    swap 3 = and
    swap 3 = and
    if
      me @ "Test passed." notify
    else
      me @ "Test failed." notify
    then
;
